//Sintaxe ES6
//export default function(...nomes) {
//    return nomes.map(nome => `Boa semana ${nome}!`)
//}

module.exports = function(...nomes) {
    return nomes.map(nome => `Boa semana ${nome}!`)
}